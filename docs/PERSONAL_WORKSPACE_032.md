# Personal workspace 0.32.0

Authenticated Atlas users can save accessible resources and named search
criteria in My Atlas. Dashboard reads reapply the active organization boundary,
so a saved identifier never bypasses current access.

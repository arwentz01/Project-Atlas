# Requirement workspace polish slices 0.94-0.98

Atlas 0.94 through 0.98 completes the next proposal-workflow polish pass:

- 0.94 renders proposal lifecycle timeline events in the requirement detail admin UI;
- 0.95 adds a full requirement workspace REST endpoint;
- 0.96 annotates source impact review rows with open proposal blockers;
- 0.97 adds dashboard proposal lifecycle summary counts;
- 0.98 exposes workspace JSON links and release-gate coverage for the expanded workflow.

These slices keep requirement proposal work internal-only and preserve the rule that draft proposals must be resolved before source-impact review closeout.

# Resource history 0.29.0

Authorized editors can inspect all accessible immutable versions, open a
version's governance workspace, clone an existing version into a new draft, and
archive the resource. Revision creation copies source citations while assigning
a new version identifier and sequential version number.

Archived resources are excluded from normal search and published detail reads.

# Organization branding 0.27.0

Users with `atlas_manage_branding` can configure the active organization's
display name, primary color, WordPress image attachment, contact block, and
approved footer. Values are bounded and validated before the existing branding
profile is replaced.

The Organizations screen includes a live server-rendered preview. Patient
Resource rendering continues to resolve the saved profile at display time so
reviewed clinical content remains separate from organization presentation.

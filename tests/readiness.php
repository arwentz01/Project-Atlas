<?php
declare(strict_types=1);define('ATLAS_PLATFORM_DIR',dirname(__DIR__).'/plugin/atlas-platform/');require ATLAS_PLATFORM_DIR.'src/Autoloader.php';Atlas\Platform\Autoloader::register();use Atlas\Platform\Core\Diagnostics\ReadinessEvaluator;function ready_expect(bool$c,string$m):void{if(!$c){throw new RuntimeException($m);}echo"PASS: {$m}\n";}$e=new ReadinessEvaluator();$ready=$e->evaluate(['release'=>true,'migrations'=>true,'capabilities'=>true]);ready_expect($ready['status']==='ready'&&$ready['failed_checks']===[],'all readiness gates produce ready status');$failed=$e->evaluate(['release'=>true,'migrations'=>false,'capabilities'=>false]);ready_expect($failed['status']==='not_ready'&&$failed['failed_checks']===['migrations','capabilities'],'readiness reports every actionable failed gate');echo"All readiness tests passed.\n";
